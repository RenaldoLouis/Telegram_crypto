# Quarterly Deep Analysis — 2026-05-16 10:18 UTC
_Based on 93 evaluated trades. Model: claude-sonnet-4-6_
_Tokens: 8760 input + 2000 output_

# Non-Obvious Pattern Analysis: 7 Actionable Insights

---

**1. MFE Betrayal Pattern: Trades That Reach T1 Still Lose at Nearly 50% Rate**

Of 93 trades, 53 reached 0.5R MFE and 31 hit T1 — yet the system shows 57 losses. Cross-referencing the recent trades reveals a critical pattern: multiple trades show `target_1_hit: true` AND `stop_hit: true` (e.g., DOGEUSDT 20260514_0721, ADAUSDT 20260514_0646, ADAUSDT 20260513_1506). The trade reaches T1, presumably moves stop to breakeven or partial, then reverses and stops out for -1.0R anyway. This means the trade management after T1 is actively destroying value — a trade that hit T1 should never return -1.0R.

**Impact**: At least 8-10 trades in the dataset are phantom wins — they *worked* directionally but the exit logic failed them.

**Recommendation**: When T1 is hit, immediately move stop to entry (breakeven), not to a trailing level. A trade that hit T1 and then stopped at -1.0R is a system logic error, not a market failure. Audit and fix the stop-management code for the T1→stop sequence.

---

**2. Run-Level Clustering: Losses Come in Synchronized Batches Across All Setups**

Looking at run_tag `20260512_0355`: all 3 stop-outs hit within 40-46 candles, with MFEs of 0.17, 0.35, and 0.52 — the market never moved in the setup's direction at all. Same pattern for `20260513_0233`: ADAUSDT and ENAUSDT both stopped in exactly 40 candles with MFE < 0.6R. When the *first* setup in a run fails fast (< 50 candles, MFE < 0.6R), the remaining setups in that same run fail at an extremely high rate.

**Impact**: The system is scanning during macro regime periods (high correlation selloffs, or pre-news consolidation) where *all* setups fail simultaneously. These aren't individual trade failures — they're run-level environment failures.

**Recommendation**: Add an intra-run circuit breaker. If the first triggered setup stops out in < 60 candles with MFE < 0.5R, cancel or flag the remaining open setups from that same run for manual review. The regime has invalidated the scan's assumptions.

---

**3. ADAUSDT Is a Silent Drain — 0/3 Wins But Keeps Getting Selected**

ADAUSDT appears in 3 of the last 8 run tags (20260513_0233, 20260513_1506, 20260514_0646) and loses every time (-3.0 RR total). More tellingly, its MFE pattern is inconsistent: 0.57R (bad setup), 2.26R (good structure, bad exit), 1.83R (decent MFE, still stopped). The symbol reaches favorable levels but doesn't sustain moves — suggesting it has high mean-reversion tendency at key levels, or its ATR-based stop sizing is systematically miscalibrated for this asset's volatility profile.

**Impact**: The screener keeps selecting it despite a 0% win rate, burning 3R of losses. It is not a data-thin anomaly — it's a structural underperformer.

**Recommendation**: Add ADAUSDT to a temporary exclusion list (minimum 15 additional trades before re-evaluation). Separately, if it must be included, require tf_confluence ≥ 4 AND MFE historical pattern showing sustained moves, not the current whipsaw profile.

---

**4. tf_confluence = 4 Loses When Paired With Rank #1, Wins When Paired With Rank #2+**

DOGEUSDT consistently shows tf_confluence = 4 and is ranked #1 in multiple runs — yet its recent record is mixed (wins on 20260513_0233 rank 5, 20260513_1506 rank 1, losses on 20260514_0721 rank 1, 20260514_0646 rank 1). The losses all share rank = 1 AND tf_confluence = 4. Meanwhile XRPUSDT with tf_confluence = 3 at rank 2-3 wins consistently. High confluence at rank #1 appears to indicate the *most obvious* crowded setup — not the best setup. The market fades the obvious level.

**Impact**: This is the deeper mechanic behind the known Rank #1 underperformance. It's not just rank — it's rank #1 *with* high confluence, which signals a consensus trade that gets faded.

**Recommendation**: When a setup is rank #1 with tf_confluence ≥ 4, explicitly demote it to rank #2 or #3 in output ordering, or require an additional non-correlated confirmation (e.g., volume spike at the pullback level, not just structural alignment). The crowded setup needs a higher bar.

---

**5. Candle Duration Bifurcation: Trades Exit at ~40 Candles (Failures) or ~187-192 Candles (Mixed) — Nothing in Between**

Exit candle counts cluster into two distinct groups: fast failures at 39-48 candles (all the clean wins: XRPUSDT 2.11R in 39c, 2.73R in 48c) and slow resolutions at 187-192 candles (mix of wins and losses). There are almost no trades exiting in the 50-186 candle window. The fast exits that win are genuine momentum trades; the fast exits that lose are immediate invalidations. Trades that survive past 100 candles without hitting T1 are drifting, not trending.

**Impact**: A trade still open at candle 100 without a T1 hit has essentially no edge — it's just occupying capital waiting to expire or stop out. The 187-192 candle exits represent the system's time limit forcing closure on stalled positions.

**Recommendation**: Implement a time-based stop at 100 candles: if T1 has not been hit by candle 100, close at market or tighten stop to 0.25R below entry. This converts slow losers to breakeven/small-loss exits and frees capital for the next run's higher-momentum setups.

---

**6. The Prediction Gap Is Not Random — It's Systematically Biased by Setup Type**

The aggregate prediction gap is enormous: 196.8R predicted vs -3.1R actual across 93 trades. But `failed_breakout` and `post_liquidation` each show -3.0R on 3 trades with 0 wins — meaning the model predicts ~2R for setups that go immediately to stop every single time. These aren't miscalibrated by a little; the model assigns the same predicted_rr (~2.1R) to these setup types as to trend_pullback, despite them having catastrophically different actual outcomes.

**Impact**: The model cannot distinguish between setup types when assigning predicted_rr. This means predicted_rr is useless as a ranking signal — it's nearly constant (~2.0-2.4R) regardless of setup quality.

**Recommendation**: Predicted_rr should be adjusted by a setup-type multiplier before use in ranking: trend_pullback × 1.0, range_breakout × 1.2 (small sample, positive), failed_breakout × 0.0 (discard entirely), post_liquidation × 0.0 (discard entirely). Do not use raw predicted_rr for inter-setup comparison — normalize it by historical setup-type performance.

---

**7. Simulated T1 Data Reveals the System Is Misidentifying Its Own Winners**

Of 16 trades in the simulated T1 sample: 14 hit 0.75R and 12 hit 1.0R — but only some of these are marked as actual wins. Several trades are classified as losses (`won: false`) despite reaching 1.0R+ MFE because they later reversed. This means the current win/loss binary is measuring exit-management quality, not setup quality. The setup itself has a ~75% success rate at the 1.0R level (12/16 in sim sample), but the reported win
